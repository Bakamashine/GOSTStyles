\RequirePackage{array}
\RequirePackage{tabularx}
\RequirePackage{geometry}
\RequirePackage{eso-pic}

\geometry{
	a4paper,
	left=25mm,
	right=5mm,
	top=5mm,
	bottom=25mm
}

% \geometry{
% 	a4paper,
% 	left=25mm,
% 	right=25mm,
% 	top=25mm,
% 	bottom=25mm
% }

% \usepackage[left=2cm,right=1cm,top=2cm,bottom=2cm]{geometry}

\usepackage{multirow}
\usepackage{tabularray}

\newcommand{\BackgroundTopTable}{%
    \begin{tblr}[t]{
        width=\textwidth,
        colspec={|c|c|c|c|c|X|c|}
        }
        \hline
        & & & & &
        \SetCell[r=3]{c} {09.02.07 Ф 2484 25\\ КП-ПЗ} &
        \SetCell[r=2]{c,2em}Лист\\
        \cline{1-5}
        & & & & & & \\
        \cline{1-5}\cline{7-7}
        Изм. & Страница & № Документ. & Подп. & Дата & & \thepage \\
        \hline
    \end{tblr}
    }

% Рамка вокруг текстовой области
\AddToShipoutPictureBG{%
    \AtTextLowerLeft{%
        \framebox(\textwidth,\textheight){}
    }%
}

% Нижняя таблица (штамп) — стыкуется с рамкой текста
\AddToShipoutPictureBG{%
    \AtTextLowerLeft{%
        \BackgroundTopTable
    }%
}
